<UserControl x:Class="ClockIn.Client.MainMoudels.Views.UserInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClockIn.Client.MainMoudels.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock Text="{Binding userRole,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Foreground="#d0bfff" FontSize="14" FontWeight="SemiBold"
        TextAlignment="Center"/>
        <TextBlock Text="{Binding userName,UpdateSourceTrigger=PropertyChanged}" Foreground="White" FontSize="22" FontWeight="SemiBold"
        TextAlignment="Center" Margin="5 25 0 10"/>
    </Grid>
</UserControl>
